= link_to product_path(product), class: "show-link", data: {"turbolinks" => false} do
  .product-box
    .product-box__top
      .product-box__top__image
        = image_tag product.images.first.name.to_s, size: '188x188', class: 'product-image'
        .product-price
          ¥
          = product.price.to_s(:delimited)
    .product-box__description
      %span 
        = product.name